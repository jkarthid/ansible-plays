---
- hosts: all
  user: jayakarthi
  sudo: true
  gather_facts: no
  update_apt_cache: true
  tasks:
     - name: System update
       apt: update_cache=yes
       sudo: yes

#    - name: System update
#      command: /usr/bin/apt-get update
#      sudo: yes
